name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.11
  - pip
    # Testing
  - biopandas
  - codecov
  - matplotlib
  - numpy
  - pandas
  - pytest
  - pytest-cov
  - seaborn
  - scikit-learn

  # Pip-only installs
  #- pip:
  #  - codecov

