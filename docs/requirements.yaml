name: docs
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
    # Package depends
  - numpy
  - matplotlib
  - pandas
  - matplotlib
  - pandas
  - seaborn
  - scikit-learn

    # Pip-only installs
  - pip:
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
