name: docs
channels:
dependencies:
    # Base depends
  - python=3.8
  - pip

    # Package depends
  - biopython
  - matplotlib
  - numpy
  - pandas
  - pandas
  - seaborn
  - scikit-learn

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - sphinx-autoapi
    - biopandas
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
